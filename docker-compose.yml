version: "3.9"

services:
  app:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - db

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: pogoda
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
